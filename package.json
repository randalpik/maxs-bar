{
  "name": "maxs-bar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Max's Bar — a cocktail index",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:ingredients": "vite-node scripts/gen-ingredients.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
